# OctoFit Tracker Backend

Node.js + Express + TypeScript backend scaffold for OctoFit Tracker.

## Scripts

- `npm run dev` - start backend dev server on port 8000
- `npm run build` - compile TypeScript to `dist`
- `npm start` - run compiled production server

## MongoDB

Backend expects MongoDB on port 27017 by default.
